/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "cdn.worldweatheronline.com",
      "cdn.weatherapi.com",
      "images.pexels.com",
    ],
  },
};

export default nextConfig;
